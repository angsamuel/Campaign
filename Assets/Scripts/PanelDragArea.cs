using UnityEngine;
using System.Collections;

public class PanelDragArea : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//GetComponent<RectTransform> ().sizeDelta = new Vector2 ();
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
